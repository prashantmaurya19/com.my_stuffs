# import math

# type used to getting the typename of variable like this <class 'type'>
# if __name__ == '__main__':
# 	print(type(math.gcd(3,6)))

name = 'prashant'
name2 = 'rohan'
# print(name[2:5]) #this is used to slice the string
# print(name.strip()) #this is used for trimming the string
# print(len(name)) printing the length of string
# name.lower() #converting to lowercase
# name.upper() #converting to uppercase
name.replace('r',"io") #replaceing
# "+" plus use for concation
# f"" this is a f_string
# temp = "my name is {int} and {int} is my friend".format(name,name2) used for templating of string
print(name)

# some operator in python
# ** Exponentiation operator
# // Floor divison operator
#  % modulo operator


''' 
collections in python
1. list eg = [2,3,4,5,5,6,0]
	methods of list
	1 list[index]
	2 list[start:end]
	3 list[index] = anyvalue
	4 list.append(anyvalue)
	5 list.insert(anyvalue)
	6 list.remove(existing_element)
	7 del(delete) list[index]
	8 list.pop()
	9 list.clear()

2. tuple = (2,3,4,5,8) this is unchangeable
	method of tuple
	same as list methods
3. set = {3,2,5,6,34,7,4,4}
	methods of set
	set.add(element)
	set.update(list)
	len(set)
	set.remove(element) << it throw error 
	set.discard(element)
	pop , clear , del
	all set operation : intersection , union
4. dictionary = {key:value}
	methods of dictionary
	dictionary[key] = value
	dictionary[key] getting the value
	dictionary.pop(key)
	del, clear , update

'''


# for taking user input
# variable_name = input(string)



# loops
# for element : list,tuple,set,dictionary:


# function in python
'''
def function_name:
	pass
'''

# classes in python
'''
class class_name:
	def __init__(self):
		pass
'''









