#include<iostream> //this is compilar defind header file
using namespace std;
// #include"prashant.h" >> this is a user defind header file

int main(){
    //operators in c++

    // arithmatic operators ---> + - * /

    // comparison operators --> '<' '>' '!=' '<=' '>=' '=='

    // logical operators --> && || !
    int a = 8,b = 4;
    cout<<"a / b = "<<(a>b);
    return 0;
}

