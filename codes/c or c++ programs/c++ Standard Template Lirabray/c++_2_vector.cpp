#include<iostream>
using namespace std;

/*
>>vectors
>>to initialize a vector

vector<int> v;

>>Methods
v.push_back(int);
v.emplace_back(int);
>>both above methods works same

but,
std::vector<pair<int,int>> v;
v.push_back({pair}) // carlybarces is neccessarary
v.emplace_back(pair) // carlybarces is not required

>>vector of size 5 with everyone as 100 as value
std::vector<int> v(5,100);


*/


int main(){


 	return 0;
 }
